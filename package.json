{
  "name": "meta-jungle",
  "version": "3.0.0",
  "private": true,
  "description": "Meta-Jungle Ecosystem — Web3 Loyalty, Reputation, Commerce & Campaign Infrastructure. Powered by LPanda NFT on Base.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "lint": "npm run lint --workspace apps/web",
    "type-check": "npm run type-check --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

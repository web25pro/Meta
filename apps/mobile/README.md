# apps/mobile

Meta-Jungle mobile app — **React Native + Expo + Expo Router**
(Master Prompt v3.0, Chapter 8 / Phase 4).

> **Status: scaffold only.** The mobile app targets feature parity with web
> Phases 1–3 and is scheduled for Phase 4 (Weeks 19–26). This directory reserves
> the workspace slot. It will consume the shared design tokens from
> `@meta-jungle/ui` (`tailwind-preset.cjs` / `tokens.css`) and the domain model
> from `@meta-jungle/types`.

Planned: Expo push notifications + FCM (Android), OTA updates, guild system,
Meta-Jungle ID NFT mint flow.

# Backend origin — SERVER-SIDE ONLY, never exposed to the browser.
# Used by the API proxy at src/app/api/[...path]/route.ts.
# Set this in the Vercel project settings for the admin deployment.
BACKEND_URL=https://meta-4bck.onrender.com/api/v1
